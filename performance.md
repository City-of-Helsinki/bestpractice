* frontend tracking
  * lighthouse / CLI
  * run against every new commit / PR
  * note that lighthouse will require some unknown permissions? for running. Maybe safe?
* backend tracking
  * static requests
* storage of
  * SaaS? (new relic, datadog)
  * Whisper (no metadata)
* alerts
  * time threshold
  
